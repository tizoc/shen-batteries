(module-conformance.record-load left)

(package module-conformance-left []

(define marker
  -> left)

\\ This deliberately false signature proves that the descriptor selected tc-.
(define unchecked-marker
  { number --> number }
  X -> unchecked)

)
